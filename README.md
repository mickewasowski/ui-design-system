# UI Design System

> This library will provide the following:
> Button component - supports different states and custom color
> Input component - provides a styled input component with a label
> ThemeProvider - provides light/dark theme support and a function to toggle the theme

